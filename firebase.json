{
  "functions": [
    {
      "source": "firebase/functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ],
      "predeploy": [
        "test -f \"$RESOURCE_DIR/package-lock.json\" || (echo \"Missing firebase/functions/package-lock.json. Run npm install in firebase/functions and commit the lockfile.\" && exit 1)"
      ]
    }
  ]
}
